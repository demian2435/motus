# Example of a simple rule
name: big-data-pipeline
input:
  type: webhook
  params:
    port: 8080
when:
  type: data.arrival
  metadata.size_gb: "<100"
then:
  target: dummy
  params:
    mode: batch
    priority: low
